function calcularAreaTriangulo(base, altura) {
    return (base * altura) / 2
}

console.log(calcularAreaTriangulo(12, 9))

function calcularCircunferencia(radio) {
    return 2 * Math.PI * radio
}

console.log(calcularCircunferencia(4))